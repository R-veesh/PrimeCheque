<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.ActivationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Border
            Width="400"
            Padding="32"
            CornerRadius="12"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <StackPanel Spacing="20">
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <TextBlock
                        Text="&#x1F6E1;"
                        FontSize="48"
                        HorizontalAlignment="Center" />
                    <TextBlock
                        Text="Activation Required"
                        Style="{StaticResource TitleTextBlockStyle}"
                        HorizontalAlignment="Center"
                        FontWeight="Bold" />
                    <TextBlock
                        Text="PrimeCheque must be activated before use."
                        Style="{StaticResource CaptionTextBlockStyle}"
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        TextAlignment="Center" />
                </StackPanel>

                <Rectangle Height="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Margin="0,4" />

                <TextBlock
                    Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    FontWeight="SemiBold"
                    HorizontalAlignment="Center"
                    TextWrapping="Wrap"
                    TextAlignment="Center" />

                <Button
                    Content="Activate Company"
                    Command="{x:Bind ViewModel.RequestActivationCommand}"
                    IsEnabled="{x:Bind ViewModel.IsNotActivating, Mode=OneWay}"
                    Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Height="40"
                    FontSize="16"
                    FontWeight="Bold" />
                    
                <ProgressRing 
                    IsActive="{x:Bind ViewModel.IsActivating, Mode=OneWay}" 
                    HorizontalAlignment="Center" 
                    Width="24" Height="24" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
