<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.ChequeListPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:converters="using:PrimeCheque.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter" />
        <converters:AmountFormatConverter x:Key="AmountFormatConverter" />
        <converters:DateFormatConverter x:Key="DateFormatConverter" />
    </Page.Resources>

    <ScrollViewer Padding="24">
        <StackPanel Spacing="20">
            <TextBlock Text="Cheque Register" Style="{StaticResource TitleTextBlockStyle}" />

            <!-- Filters Bar -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Header="Search" Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}" PlaceholderText="Search by payee, cheque number, memo..." />
                <ComboBox Grid.Column="1" Header="Status Filter" ItemsSource="{x:Bind ViewModel.StatusValues}" SelectedItem="{x:Bind ViewModel.SelectedStatusFilter, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                <Button Grid.Column="2" Content="Search" Command="{x:Bind ViewModel.SearchCommand}" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" />
            </Grid>

            <!-- Table -->
            <ListView ItemsSource="{x:Bind ViewModel.Cheques, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="160" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding ChequeNumber}" FontWeight="Bold" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding PayeeName}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding Amount, Converter={StaticResource AmountFormatConverter}}" FontWeight="SemiBold" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="3" Text="{Binding ChequeDate, Converter={StaticResource DateFormatConverter}}" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <Border Grid.Column="4" Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="Bold" />
                            </Border>
                            <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                <Button Content="Submit" Command="{Binding DataContext.SubmitForApprovalCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Padding="8,4" FontSize="11" />
                                <Button Content="Approve" Command="{Binding DataContext.ApproveChequeCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Style="{StaticResource AccentButtonStyle}" Padding="8,4" FontSize="11" />
                                <Button Content="Reject" Command="{Binding DataContext.RejectChequeCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Padding="8,4" FontSize="11" />
                                <Button Content="Print" Command="{Binding DataContext.PrintChequeCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Padding="8,4" FontSize="11" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
