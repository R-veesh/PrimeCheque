<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.DashboardPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:converters="using:PrimeCheque.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter" />
        <converters:AmountFormatConverter x:Key="AmountFormatConverter" />
        <converters:DateFormatConverter x:Key="DateFormatConverter" />
    </Page.Resources>

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24">
            <TextBlock Text="Dashboard" Style="{StaticResource TitleTextBlockStyle}" />

            <!-- Summary Cards -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Card 1 -->
                <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Cheques Today" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.TotalChequesToday, Mode=OneWay}" Style="{StaticResource HeaderTextBlockStyle}" />
                    </StackPanel>
                </Border>

                <!-- Card 2 -->
                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Total Issued Today" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.TotalAmountToday, Mode=OneWay, Converter={StaticResource AmountFormatConverter}}" Style="{StaticResource HeaderTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    </StackPanel>
                </Border>

                <!-- Card 3 -->
                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Pending Approvals" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.PendingApprovals, Mode=OneWay}" Style="{StaticResource HeaderTextBlockStyle}" Foreground="Orange" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="✏️ New Cheque" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.NavigateToNewChequeCommand}" Padding="16,10" />
                <Button Content="📋 View Cheque Register" Command="{x:Bind ViewModel.NavigateToChequeListCommand}" Padding="16,10" />
            </StackPanel>

            <!-- Recent Cheques Table -->
            <StackPanel Spacing="12">
                <TextBlock Text="Recent Cheques" Style="{StaticResource SubtitleTextBlockStyle}" />
                <ListView ItemsSource="{x:Bind ViewModel.RecentCheques, Mode=OneWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="16" Padding="12,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="180" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding ChequeNumber}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding PayeeName}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding Amount, Converter={StaticResource AmountFormatConverter}}" FontWeight="Bold" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3" Text="{Binding ChequeDate, Converter={StaticResource DateFormatConverter}}" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <Border Grid.Column="4" Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="Bold" />
                                </Border>
                                <Button Grid.Column="5" Content="🖨️ Print / Preview" Command="{Binding DataContext.PrintChequeCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Style="{StaticResource AccentButtonStyle}" Padding="8,4" FontSize="11" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
