<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.BankManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- List View -->
        <StackPanel Grid.Column="0" Spacing="16">
            <TextBlock Text="Bank Master Data" Style="{StaticResource TitleTextBlockStyle}" />
            <ListView ItemsSource="{x:Bind ViewModel.Banks, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="Bold" />
                            <TextBlock Grid.Column="1" Text="{Binding ShortName}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock Grid.Column="2" Text="{Binding SwiftCode}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Form View -->
        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <StackPanel Spacing="16">
                <TextBlock Text="Add / Edit Bank" Style="{StaticResource SubtitleTextBlockStyle}" />

                <TextBox Header="Bank Full Name" Text="{x:Bind ViewModel.Name, Mode=TwoWay}" />
                <TextBox Header="Short Name (e.g. COMBANK)" Text="{x:Bind ViewModel.ShortName, Mode=TwoWay}" />
                <TextBox Header="Branch Code" Text="{x:Bind ViewModel.BranchCode, Mode=TwoWay}" />
                <TextBox Header="Swift Code" Text="{x:Bind ViewModel.SwiftCode, Mode=TwoWay}" />

                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="Clear" Command="{x:Bind ViewModel.ClearFormCommand}" />
                    <Button Content="Save Bank" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SaveBankCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
