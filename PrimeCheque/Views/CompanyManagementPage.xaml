<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.CompanyManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- List View -->
        <StackPanel Grid.Column="0" Spacing="16">
            <TextBlock Text="Company Management" Style="{StaticResource TitleTextBlockStyle}" />
            <ListView ItemsSource="{x:Bind ViewModel.Companies, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                <TextBlock Text="{Binding RegistrationNumber}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="Edit" Command="{Binding DataContext.EditCompanyCommand, ElementName=RootPage}" CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Form View -->
        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <StackPanel Spacing="16">
                <TextBlock Text="Add / Edit Company" Style="{StaticResource SubtitleTextBlockStyle}" />

                <TextBox Header="Company Name" Text="{x:Bind ViewModel.Name, Mode=TwoWay}" />
                <TextBox Header="Registration No" Text="{x:Bind ViewModel.RegistrationNumber, Mode=TwoWay}" />
                <TextBox Header="Address" Text="{x:Bind ViewModel.Address, Mode=TwoWay}" />
                <TextBox Header="Phone" Text="{x:Bind ViewModel.Phone, Mode=TwoWay}" />
                <TextBox Header="Email" Text="{x:Bind ViewModel.Email, Mode=TwoWay}" />

                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="Clear" Command="{x:Bind ViewModel.ClearFormCommand}" />
                    <Button Content="Save Company" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SaveCompanyCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
