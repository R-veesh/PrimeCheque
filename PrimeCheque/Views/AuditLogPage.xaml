<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.AuditLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:converters="using:PrimeCheque.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:DateFormatConverter x:Key="DateFormatConverter" />
    </Page.Resources>

    <Grid Padding="24" ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- Audit Log List -->
        <StackPanel Grid.Column="0" Spacing="20">
            <TextBlock Text="System Audit Trail" Style="{StaticResource TitleTextBlockStyle}" />

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Header="Filter by User" Text="{x:Bind ViewModel.UserFilter, Mode=TwoWay}" PlaceholderText="User name..." />
                <TextBox Grid.Column="1" Header="Filter by Action" Text="{x:Bind ViewModel.ActionFilter, Mode=TwoWay}" PlaceholderText="Created, Printed, Voided..." />
                <Button Grid.Column="2" Content="Search" Command="{x:Bind ViewModel.SearchCommand}" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Bottom" Padding="16,8" />
            </Grid>

            <ListView ItemsSource="{x:Bind ViewModel.AuditLogs, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedLog, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Timestamp}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" VerticalAlignment="Center" />
                            <Border Grid.Column="1" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <TextBlock Text="{Binding ActionType}" Foreground="White" FontSize="11" FontWeight="Bold" />
                            </Border>
                            <TextBlock Grid.Column="2" Text="{Binding PerformedBy}" FontWeight="SemiBold" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="3" Text="{Binding ChequeId}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="11" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- State Inspector -->
        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <ScrollViewer>
                <StackPanel Spacing="16">
                    <TextBlock Text="Audit Event Inspector" Style="{StaticResource SubtitleTextBlockStyle}" />

                    <TextBlock Text="Before State (JSON):" FontWeight="Bold" />
                    <TextBox Text="{x:Bind ViewModel.SelectedLog.BeforeState, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" Height="150" FontFamily="Consolas" />

                    <TextBlock Text="After State (JSON):" FontWeight="Bold" Margin="0,12,0,0" />
                    <TextBox Text="{x:Bind ViewModel.SelectedLog.AfterState, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" Height="200" FontFamily="Consolas" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
