<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrimeCheque.Views.TemplateDesignerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrimeCheque.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- Visual Canvas Preview -->
        <StackPanel Grid.Column="0" Spacing="16">
            <TextBlock Text="Bank Template Designer" Style="{StaticResource TitleTextBlockStyle}" />

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" Header="Select Template to Edit" ItemsSource="{x:Bind ViewModel.Templates, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedTemplate, Mode=TwoWay}" DisplayMemberPath="BankName" HorizontalAlignment="Stretch" />
                <Button Grid.Column="1" Content="➕ New Template" Command="{x:Bind ViewModel.CreateNewTemplateCommand}" VerticalAlignment="Bottom" />
            </Grid>

            <!-- Visual Cheque Leaf Representation -->
            <Border x:Name="PreviewBorder" SizeChanged="PreviewBorder_SizeChanged" Height="{x:Bind ViewModel.CanvasHeight, Mode=OneWay}" Background="#F8F9FA" CornerRadius="6" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1.5" HorizontalAlignment="Stretch">
                <Grid>
                    <!-- Background Cheque Image from template_image/ -->
                    <Image Source="{x:Bind ViewModel.FullImagePath, Mode=OneWay}" Stretch="Fill" Opacity="0.85" />

                    <Canvas x:Name="ChequeCanvas">
                        <!-- Date Day -->
                        <Border Canvas.Left="{x:Bind ViewModel.DateDayPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.DateDayPxY, Mode=OneWay}" BorderBrush="Blue" Background="#330000FF" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="DD" FontSize="10" Foreground="Blue" FontWeight="Bold" />
                        </Border>
                        <!-- Date Month -->
                        <Border Canvas.Left="{x:Bind ViewModel.DateMonthPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.DateMonthPxY, Mode=OneWay}" BorderBrush="Blue" Background="#330000FF" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="MM" FontSize="10" Foreground="Blue" FontWeight="Bold" />
                        </Border>
                        <!-- Date Year -->
                        <Border Canvas.Left="{x:Bind ViewModel.DateYearPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.DateYearPxY, Mode=OneWay}" BorderBrush="Blue" Background="#330000FF" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="YYYY" FontSize="10" Foreground="Blue" FontWeight="Bold" />
                        </Border>
                        <!-- Payee -->
                        <Border Canvas.Left="{x:Bind ViewModel.PayeeLine1PxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.PayeeLine1PxY, Mode=OneWay}" Width="{x:Bind ViewModel.PayeeLine1PxW, Mode=OneWay}" BorderBrush="DarkGreen" Background="#33008000" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="PAYEE NAME LINE" FontSize="11" Foreground="DarkGreen" FontWeight="Bold" />
                        </Border>
                        <!-- Amount Words -->
                        <Border Canvas.Left="{x:Bind ViewModel.AmountWordsPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.AmountWordsPxY, Mode=OneWay}" Width="{x:Bind ViewModel.AmountWordsPxW, Mode=OneWay}" BorderBrush="Purple" Background="#33800080" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="** Sri Lanka Rupees Seventy-Five Thousand Only **" FontSize="10" Foreground="Purple" FontWeight="Bold" />
                        </Border>
                        <!-- Amount Figures -->
                        <Border Canvas.Left="{x:Bind ViewModel.AmountFiguresPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.AmountFiguresPxY, Mode=OneWay}" Width="{x:Bind ViewModel.AmountFiguresPxW, Mode=OneWay}" BorderBrush="Red" Background="#33FF0000" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="**75,000.00**" FontSize="11" Foreground="Red" FontWeight="Bold" />
                        </Border>
                        <!-- Crossing -->
                        <Border Canvas.Left="{x:Bind ViewModel.CrossingPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.CrossingPxY, Mode=OneWay}" BorderBrush="Orange" Background="#33FFA500" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="// A/C PAYEE ONLY //" FontSize="9" Foreground="Orange" FontWeight="Bold" />
                        </Border>
                        <!-- Memo -->
                        <Border Canvas.Left="{x:Bind ViewModel.MemoPxX, Mode=OneWay}" Canvas.Top="{x:Bind ViewModel.MemoPxY, Mode=OneWay}" BorderBrush="Teal" Background="#33008080" BorderThickness="1" Padding="4,2">
                            <TextBlock Text="MEMO / NOTE" FontSize="9" Foreground="Teal" FontWeight="Bold" />
                        </Border>
                    </Canvas>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Form & Coordinate Controls -->
        <ScrollViewer Grid.Column="1">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel Spacing="14">
                    <TextBlock Text="Field Positions (mm)" Style="{StaticResource SubtitleTextBlockStyle}" />

                    <TextBox Header="Bank Name" Text="{x:Bind ViewModel.BankName, Mode=TwoWay}" />
                    <TextBox Header="Series Name" Text="{x:Bind ViewModel.SeriesName, Mode=TwoWay}" />

                    <TextBlock Text="Date Day Coordinates" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,0" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <NumberBox Grid.Column="0" Header="Day X (mm)" Value="{x:Bind ViewModel.DateDayX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="1" Header="Day Y (mm)" Value="{x:Bind ViewModel.DateDayY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                    </Grid>

                    <TextBlock Text="Date Month &amp; Year Coordinates" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,0" />
                    <Grid ColumnSpacing="8" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <NumberBox Grid.Row="0" Grid.Column="0" Header="Month X (mm)" Value="{x:Bind ViewModel.DateMonthX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Row="0" Grid.Column="1" Header="Month Y (mm)" Value="{x:Bind ViewModel.DateMonthY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Row="1" Grid.Column="0" Header="Year X (mm)" Value="{x:Bind ViewModel.DateYearX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Row="1" Grid.Column="1" Header="Year Y (mm)" Value="{x:Bind ViewModel.DateYearY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                    </Grid>

                    <TextBlock Text="Payee Line Coordinates" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,0" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <NumberBox Grid.Column="0" Header="Payee X" Value="{x:Bind ViewModel.PayeeLine1X, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="1" Header="Payee Y" Value="{x:Bind ViewModel.PayeeLine1Y, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="2" Header="Width" Value="{x:Bind ViewModel.PayeeLine1W, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                    </Grid>

                    <TextBlock Text="Amount Words Coordinates" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,0" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <NumberBox Grid.Column="0" Header="Words X" Value="{x:Bind ViewModel.AmountWordsX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="1" Header="Words Y" Value="{x:Bind ViewModel.AmountWordsY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="2" Header="Width" Value="{x:Bind ViewModel.AmountWordsW, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                    </Grid>

                    <TextBlock Text="Amount Figures Coordinates" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,0" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <NumberBox Grid.Column="0" Header="Figures X" Value="{x:Bind ViewModel.AmountFiguresX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="1" Header="Figures Y" Value="{x:Bind ViewModel.AmountFiguresY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Column="2" Header="Width" Value="{x:Bind ViewModel.AmountFiguresW, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                    </Grid>

                    <TextBlock Text="Crossing &amp; Memo Coordinates" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,0" />
                    <Grid ColumnSpacing="8" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <NumberBox Grid.Row="0" Grid.Column="0" Header="Crossing X" Value="{x:Bind ViewModel.CrossingX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Row="0" Grid.Column="1" Header="Crossing Y" Value="{x:Bind ViewModel.CrossingY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Row="1" Grid.Column="0" Header="Memo X" Value="{x:Bind ViewModel.MemoX, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                        <NumberBox Grid.Row="1" Grid.Column="1" Header="Memo Y" Value="{x:Bind ViewModel.MemoY, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                    </Grid>

                    <Button Content="💾 Save Template" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SaveTemplateCommand}" HorizontalAlignment="Stretch" Margin="0,16,0,0" />
                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Foreground="Green" FontWeight="Bold" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Page>
